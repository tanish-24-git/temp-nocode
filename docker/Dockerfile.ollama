FROM ollama/ollama:latest

# Pre-pull TinyLlama model during build (NOT at runtime)
# This ensures zero delays when starting the container
RUN ollama serve & \
    sleep 5 && \
    ollama pull tinyllama && \
    pkill ollama

EXPOSE 11434

CMD ["ollama", "serve"]
